let marks = 80;

function marks() {
    if (marks > 90 && marks < 100) {
        console.log(AGrade);
    }
    else if (marks > 80 && marks < 89) {
        console.log(BGrade);
    }
    else if (marks > 70 && marks < 79) {
        console.log(CGrade);
    }
    else if (marks > 60 && marks < 69) {
        console.log(dGrade);
    }
    else{
        
    }
}